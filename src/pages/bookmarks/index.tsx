import Head from "next/head";
import { ReactElement } from "react";
import { NextPageWithLayout } from "@app-types";
import { MainLayout } from "layouts/main-layout";
import { GetServerSideProps } from "next";
import { checkAuthMiddleware } from "@modules/authentication";

export const getServerSideProps: GetServerSideProps<{}> = checkAuthMiddleware(
  async (context, user) => {
    return { props: {} };
  },
);

interface PageProps {}

const Page: NextPageWithLayout<PageProps> = ({}) => {
  return (
    <>
      <Head>
        <title>Bookmarks | Daiko</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <main></main>
    </>
  );
};

Page.getLayout = (page: ReactElement) => {
  return <MainLayout title="Bookmarks">{page}</MainLayout>;
};

export default Page;
